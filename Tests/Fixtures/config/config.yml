framework:
    test: true
    secret: test
    templating:
        engine: [twig, php]
    router:
        resource: "%kernel.root_dir%/config/routing.yml"

services:
    test.rest.multiple:
        class: Tests\Fixtures\FooBundle\Controller\RestController
